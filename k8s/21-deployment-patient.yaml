apiVersion: apps/v1
kind: Deployment
metadata:
  name: patient
  namespace: healthcare
  labels:
    app: patient
spec:
  replicas: 2
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: patient
  template:
    metadata:
      labels:
        app: patient
      annotations:
        seccomp.security.alpha.kubernetes.io/pod: runtime/default
    spec:
      serviceAccountName: healthcare-patient
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: patient
          image: healthcare-micro-clean/patient:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            - name: NODE_ENV
              value: production
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: healthcare-secrets
                  key: jwt-secret
            - name: JWT_ISSUER
              value: healthcare-micro-clean
            - name: JWT_AUDIENCE
              value: healthcare-internal
            - name: AUTH_SERVICE_URL
              value: http://auth:8000
            - name: SERVICE_TOKEN_SECRET
              valueFrom:
                secretKeyRef:
                  name: healthcare-secrets
                  key: service-token
            - name: DB_HOST
              value: patient-db
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: patient
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: healthcare-db-patient
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: healthcare-db-patient
                  key: password
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 10001
            capabilities:
              drop:
                - ALL
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: patient
  namespace: healthcare
  labels:
    app: patient
spec:
  type: ClusterIP
  selector:
    app: patient
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
